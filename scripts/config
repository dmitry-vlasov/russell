#!/bin/bash 
 
## The relative path to the directory, containing binary executables: 
binpath='../build/relWithDebInfo/bin'
# binpath='../build/debug/bin'
# binpath='../build/release/bin'
 
## The relative path to the directory, containing math sources:
mathpath='../math'
#mathpath='../../../math'

## Run executables with valgrind memcheck
#memcheck='yes'

## Print command with arguments before execution
output_command='yes'

source run_check
